# Трекер прогресса

Обновлять после значимого изменения. План, компиляция и реализованное поведение — разные состояния.

## Объединение main `6a62501` — 23.09.2026

- Получены этапы09/09a и14/14a: задачи импорта/расчёта, новые API запусков/review/approval/export и две SQL-миграции. Конфликты README и трекера согласованы с сохранением результатов прежних проверок. Ниже находится историческая приёмка до этого обновления; её статусы не описывают новую интеграцию.
- Сборка, применение миграций и HTTP-проверка обновлённого Docker выполняются отдельно; присутствие кода не считается полной приёмкой.

## Последняя интеграция и проверка — 23.09.2026

- По просьбе пользователя удалён стартовый `app/favicon.ico`, в `app/layout.tsx` задан прозрачный значок. Финальная Docker-сборка и миграции прошли. Повтор расчётных тестов внутри Docker Node24.21 дал то же расхождение:146/147, поэтому проблема не ограничена macOS. Ожидаемое значение и формулы не подгонялись. В браузере390px работают все4нижние иконки без видимых подписей и горизонтальной прокрутки.
- Повторно получен и объединён `main` `8f7f90d` с исправлениями связки07→08. Утраченную при объединении строку импорта `fileURLToPath` в тестовом loader восстановили. На Node24/macOS прошли146/147 тестов расчёта; одна точная проверка получила `1027882215.44612679` вместо `1027882215.44612681`. Формулы и ожидаемый результат не менялись. Возможная зависимость `Math.pow` от среды ещё требует подтверждения; общий набор нельзя считать зелёным.
- На Docker проверены загрузка `/` без JS-ошибок, обязательная причина правки, сохранение126,5 как126.5, проверка двух поставщиков, явное утверждение и предпросмотр пяти строк без нулевой позиции. Это локальное демо, не серверный заказ. Lint и TypeScript после объединения прошли.
- Разбор настоящих исходников:11/12 книг приняты, `MOQ ИЭК` отклонён из-за внешних Excel-связей; все исходные байты сохранены. Публикации данных в БД нет. Подробности в `data-inventory.md`.

## Текущий статус сохранения источников — 23.09.2026

- Оба исходных ZIP и 12 XLSX сохранены побайтово в `.agent-loop/source-data/` вне Git. В контейнере `/app/local-source-data` смонтирован только для чтения; SHA-256 всех 14 файлов совпали с исходниками. Это сохранение, не публикация DatasetVersion: в текущей PostgreSQL 18 после миграций 0000/0001 счётчики проектов, импортов и наборов данных равны нулю.
- Структурная инвентаризация: 14 листов, 261 264 строки с ячейками, 2 285 515 ячеек, 6 080 формул, 73 497 ячеек типа ошибки. Эти числа не означают принятие бизнес-строк. Семантика источников не домысливалась; состояние работы с ними — `preserved_unmapped`.
- Лимит отдельной записи XLSX поднят с 64 до 96 MiB для XML-листа динамики IEK размером 88 043 249 байт; остальные лимиты оставлены. Локально прошли 24/24 теста импорта и production Docker build. Это не проверка успешной нормализации всех 12 книг. В Docker Node 24.21 и PostgreSQL 18, приложение отвечает на порту 3000; Clerk и живые AI-сервисы не проверены. Полный импорт, расчёт, review/export и E2E на данных партнёра остаются pending.

## Последняя приёмка B — 23.09.2026, около17:18Алматы

- Получен `main` `3ea1770` с07/08. В общем checkout прошли215/215 тестов Node24:44 B,60 серверных,110 forecast/replenishment,1 реальная DB-проверка Liveblocks. Исправление тестового loader и fixture08 не меняет расчётные формулы. Временная PostgreSQL, без Docker/облачных данных; DB-наборы запускались последовательно.
- Финальные lint, TypeScript и Webpack production build прошли. Browser: `/projects` и реальный `/workspace?projectId=UUID` закрыты при отсутствии Clerk; неверныйUUID показывает безопасную ошибку; API проектов/Liveblocks возвращают503 без сессии/конфигурации. В демо390px все4 нижние иконки переключают разделы, видимых названий нет, переполнения/ошибокJavaScript нет. Более ранняя02A-приёмка десктопа/правок/подтверждения остаётся в истории ниже.
- B03: серверная идентификация и страницы готовы в коде; liveClerk pending. B12: выбранная02A-оболочка принята как синтетическое рабочее место. B10/NVIDIA: адаптеры и безопасные fallback протестированы, но Trigger/сохранениеAI-артефактов/живые вызовы не подключены. OpenAI по-прежнему запрещён пользователем.
- B11: добавлен действующий auth-route с PostgreSQL owner/run/project lookup; проверены свой/чужой/другойпроект/архив. Публикация из задач09 и живойLiveblocks pending. B13: реальные список/создание проектов и чтение наборов черезAPI06 реализованы; скрытие данных/отмена запросов при смене сессии. Авторизованная браузерная проверка требуетClerk/DB. Импорт/расчёт/история ещё не подключены.
- B14a и полная B15 не закрыты: в полученномmain отсутствуют09/09a/14; серверный путь расчёт→review→approve→CSV и полныйM1–M5E2E не проверены. Все конкретные точки передачи, сигнатуры и расхожденияDTO описаны в [b-integration-handoff.md](b-integration-handoff.md). Новые зависимости не добавлялись.

Ниже сохранены исторические результаты отдельных веток; сведения о неподключённых зависимостях и старые числа тестов не заменяют этот текущий срез.

### 07 — Прогноз регулярного спроса

- 23.09.2026: **ядро реализовано и предметно проверено; общая сборочная приёмка заблокирована окружением**. После pull `origin/main` (`89f65b7`) владелец контракта добавил семантику внешнего прироста. Его `lib/contracts/calculation.ts` сохранён без правок: SHA-256 `d59689058998413bf5ec0259c4ac8cf7090407cd5b4aa5c1b33639c02f6c369c` до/после совпадает.
- `lib/domain/forecast/index.ts` экспортирует `forecastDemand: ForecastDemand` и `sumDailyForecast`. Реализованы известная экспозиция и разделение месячной/транзакционной истории, объединение событий, документные/клиентские выбросы, объединение stockout-интервалов, сезонные индексы, устойчивый ограниченный тренд, календарный якорь и явная семантика внешнего прироста. Нет сети, БД, фоновых задач, текущего времени и случайности. Неизвестные возвраты/экспозиция дают явную недоступность, а не выдуманные числа.
- **40/40 `node:test` прошли** через `npm run test:forecast`; результаты проверяются `ForecastResultSchema`. Покрыты все предметные пункты «Проверка готовности»: повтор/перестановка/разбиение, сезонный пик10×, устойчивый рост против всплеска, M4≤5% для100× и20документов, постоянный крупный клиент, stockout/пересечения/нехватка сравнений,11/12/13.2 и календарь месяцев, внешний прирост/короткая история/неполный сентябрь, отсутствие утечки будущего. Дополнительно проверены полнота/режимы, отрицательные коэффициенты, месячные агрегаты без двойного учёта и неизменность входов.
- Синтетический holdout: факт930; прогноз929.999999749; простое среднее349.466119097; абсолютные ошибки≈0.000000251 и580.533880903. Отчёт в `tests/fixtures/forecast/README.md`; это один искусственный сценарий, не утверждение промышленного качества.
- Финальные `npm run lint`, строгий TypeScript для `lib/domain/forecast/*.ts` и `git diff --check` прошли. Общий `npx tsc --noEmit` не проходит из-за уже отсутствующих Drizzle/pg/AWS/Clerk/ExcelJS и связанных типов. `npm run build` остановлен Turbopack: локальный `node_modules` ссылается за корень worktree. Эти проверки не объявляются успешными; исходные зависимости/Next-конфигурация не менялись.
- По прямому запросу добавлены `tests/domain/load-typescript.mjs` и только npm script `test:forecast`; прочие изменения ограничены каталогами forecast и этим подразделом. Никаких коммитов/push. Логи: `.omx/verification/forecast/`.
- Team `implement-forecast07-e0de6244`:5 исполнителей `gpt-6-astra` / `xhigh`,5 ACK, панели `%11`–`%15`;6 задач завершены,0 pending/in_progress/failed. Распределение:1—очистка/stockout;2—модель/сумма;3—оркестрация/полнота;4—фикстуры/приёмка;5—loader/численные/граничные тесты. Дополнительных агентов нет. Лидер проверил интеграцию; Team штатно остановлен, его runtime-state и6 временных worktree удалены после завершения всех задач. Реализация и проверка уложились в15минут после снятия блокировки.

## Текущий этап

**14 / 14a — в работе (23.09.2026).** Серверная проверка, явное утверждение, демонстрационный CSV и подключение UI; пять Team-работников GPT-6-Astra/xhigh, лимит пять минут. Полная приёмка будет отмечена только по фактическим проверкам.

**04-data-model — код направления A реализован и проверен локально.** Передача B: package/lock, подключение03/Clerk и smoke-test реальных RDS/S3. Эти внешние интеграции не объявляются завершёнными. Следующий этап A —05, в этой задаче не начинался.

## Текущая цель

Довести перечисленные пробелы05 до критериев спецификации, принять зависимости в package/lock у B и повторить интеграционную приёмку. До этого05 не отмечать завершённым;06/09a не считать реализованными.

## Завершено

### Реализация до анализа хакатона

- **01-design-system:** настроен shadcn/ui (`base-nova`, neutral), добавлены Button/Card/Dialog/Input/Tabs/Textarea/ScrollArea, lucide-react и `cn()` из пакета `cn`. Исправлена переменная Geist Sans, серверный `dark` включён постоянно. Ранее проверены сборка/типы и временный маршрут компонентов; браузерный скриншот не получен из-за системных зависимостей Chromium.
- **02-editor:** `components/editor/editor-navbar.tsx` (фиксированная высота, три секции, переключатель иконки), `project-sidebar.tsx` (плавающий, анимированный, inert при закрытии, вкладки/пустые состояния), README композиции будущих диалогов. Три native-субагента завершили файлы. Ранее прошли `npm run lint`, `npx tsc --noEmit`, `npm run build` и временные проверки серверного рендера/обработчиков. Браузерная анимация/клавиатура не проверялись. Компоненты ещё не подключены к главной странице; их прежние английские подписи локализуются в12, Clerk — в03.

### Документальная подготовка

- Прочитан кейс закупок, исследованы 2 ZIP / 12 XLSX / 14 листов. Инвентаризация — `data-inventory.md`; ошибки/пропуски не приняты за нули, анонимность полностью не подтверждена.
- Шесть канонических инструкций наполнены: обзор, архитектура, UI, стандарты кода, процесс, трекер. `architecture.md` оставлен ссылкой на `architecture-context.md`, чтобы устранить прежнее несовпадение с AGENTS.md.
- План00 и спецификации03–15, включая09a/14a, описывают доступ, PostgreSQL/S3, импорт, математику, Trigger, OpenAI, Liveblocks, русский UI, утверждение/экспорт и приёмку. Это план, не отметки реализации.
- Три субагента: анализ требований/архивов; интерфейс; интеграции и официальная документация. Ведущий повторно сверил контракты и исправил замечания: manifest-aware идемпотентность, версии справочников, хранение месячной истории, временной якорь тренда, приоритет срочности, отдельная полнота расчёта, отложенная валидация импорта.

### Разделение работы двух участников

- Создан [план параллельной работы](feature-specs/parallel-work-plan.md). A:04,05,06,07,08,09,09a,14; B:03,10,11,12,13,14a,15. Завершённые01/02 не назначаются заново.
- Закреплены исключения внутри общих каталогов: auth/API-envelope у B, бизнес-контракты/репозитории у A, дочерняя ИИ-задача и realtime у B. Вызовы адаптеров в оркестратор добавляет A. Общие конфигурации, зависимости и контекст ведёт B по запросам обоих участников.
- Приёмка15 разделена по файлам: генератор, фикстуры, предметные/серверные проверки у A; E2E, README и сводный отчёт у B. Статусы A передаются B для обновления этого трекера после каждого значимого изменения.
- В00 добавлены владельцы, README и правила процесса ссылаются на распределение. Функциональный объём спецификаций и архитектура не менялись; код приложения не изменялся.

## Результат 04 и передача

-25 предметных таблиц PostgreSQL, составные FK внутри проекта/снимка, уникальность и атомарный DispatchIntent. SQL-миграция с checksum/advisory-lock runner и согласованный Drizzle baseline; автоматических миграций при сборке нет.
- Строгие Zod DTO, numeric(30,8) ↔ канонические строки, UTC-даты, явная полнота/происхождение и immutable configuration. Публикация нормализованных строк и ready атомарны; после ready нельзя изменить или дополнить снимок.
- Репозитории проверяют владельца/архив, payload/hash и конфликты повторов. Результат не изменяется review; утверждение привязано к точной редакции/хешу. Этап14/API не реализован.
- Приватный S3-адаптер: серверный ключ, SHA-256/HEAD, запрет перезаписи и public ACL,25MiB/300с, обязательные серверные callback. Настоящий bucket/Clerk не подключён.
- Три Team-работника действительно использовали `gpt-6-sol` / `high`;4 задачи (включая согласование границ) завершены,0 ошибок задач. Team штатно остановлен, рабочие копии удалены после сохранения результата; коммитов нет.
- Ведущий обновил контекст по прямому запросу пользователя. Общие package/lock/auth/API-envelope/app/UI не изменялись. [Точные зависимости](../scripts/data-model-dependencies.json) и [инструкция передачи](feature-specs/04-data-model-decisions.md) подготовлены для B.

## Далее

1. B добавляет зависимости04 в своей ветке и связывает проверенную серверную личность03 с репозиториями/S3 callback. Повторить проверки на объединённом дереве.
2. Проверить настоящий RDS/S3, IAM и закрытость bucket при выдаче доступов; никакие платные ресурсы/данные партнёра этой задачей не создавались/не передавались.
3. Завершить перечисленные пробелы05. Импортёр/CLI реализованы частично; прогноз, HTTP API и фоновые задачи в рамках05 не добавлялись.
4. Продолжать дальнейшие этапы по [разделению файлов](feature-specs/parallel-work-plan.md), не дублируя работу B.

## Подтверждённые решения

- PostgreSQL и приватное S3 — явный выбор пользователя. По поручению пользователя для04 выбраны Drizzle/node-postgres, AWS RDS и S3 в eu-central-1; это технический выбор, не созданные ресурсы или разрешение передавать данные партнёра.
- Clerk, Trigger.dev, Liveblocks, OpenAI — выбранные интеграции, ещё не установлены в package.json.
- **Только русский интерфейс и документация**; технические идентификаторы остаются английскими.
- Детерминированное ядро считает заказ; OpenAI только объясняет, Trigger выполняет, Liveblocks показывает активность. PostgreSQL — единственный источник бизнес-состояния.
- Пользователь проверяет/меняет/утверждает точную редакцию; отправки поставщику в начальной версии нет.
- Проект одного владельца; организации/шаринг/чат/автономные закупки вне начального объёма.

## Открытые вопросы и блокировки

| Вопрос | Влияние / этап |
| --- | --- |
| Доступы к выбранным RDS/S3, реальная конфигурация закрытого bucket, передача03 и package/lock от B | Реальные интеграции04; инструмент/регион выбраны, ресурсы не созданы |
| Предельные размеры книг/архивов, сроки хранения, удаление, разрешение передавать агрегаты внешним сервисам | 04–06,09a,10,11; без согласования только синтетика |
| Семантика документов/отрицательных количеств; значения `t=e`/пустых кешей/`#N/A` | Реальная нормализация05 |
| Обезличенные стабильные ID клиентов, дневные интервалы отсутствия, справочник сроков, текущие остатки IEK | Реальные M1/M3/M4; демонстрация синтетикой не доказывает полноту партнёрских данных |
| Значения категорий, `СС реал`, представление роста/сезонности; где V2/материальная ведомость | 05/07/08; не угадывать |
| Единицы заказа, точность/шаг, конверсии бухта↔метр, минимум↔кратность, приоритет источников | 04/05/08; MOQ пока необязательная метаинформация |
| Часовой пояс, календарь поставок, сроки/период пересмотра/страховка по категориям | 07/08; предложенные конвенции тестировать и подтвердить |
| Модель OpenAI, бюджет, лимиты токенов/повторов/параллелизма; аккаунты Trigger/Liveblocks | 09–11; модель не выбирать по памяти |
| Точная схема импорта1С, кодировка/десятичный разделитель/совместимость экранирования | 14/15; пока только явно демонстрационный CSV |

## Проверка 04

-27 тестов `node:test` прошли:8 групп контрактов,6 репозиторных сценариев на настоящей PostgreSQL18.4, миграционные rollback/concurrency/checksum, соответствие составных FK Drizzle/SQL,2 server-only границы и9 S3-mock сценариев.
- Включены параллельные повторы, межпроектные/межверсионные связи и scope, decimal overflow/точность, неизменность исходного результата, stale review, запрет diagnostic approval, архив, атомарная публикация и безопасные события. Project/SourceObject/Import/DatasetVersion/CalculationRun/DispatchIntent прочитаны из БД и проверены DTO.
- `npm run lint`, `npx next typegen`, `npx tsc --noEmit`, `npm run build` прошли; `npx drizzle-kit generate` — без изменений. `git diff --check` и отсутствие правок B-файлов проверены.
- Локальные raw-логи: `.omx/verification/data-model/final/`. БД не подменяется SQLite; S3 проверен имитацией, не настоящим AWS. Clerk/живые RDS/S3, API/E2E и реальные данные партнёра не проверены. Зависимости временно установлены no-save; чистый checkout требует передачи package/lock от B.

### Историческая проверка документационной подготовки

После разделения работ прошли `python3 .omx/verification/check-context.py` (6 канонических инструкций, 35 Markdown-файлов, 17 этапов, без ошибок) и отдельная проверка распределения: все15 предстоящих спецификаций назначены ровно один раз, A=8/B=7, владельцы совпадают с00, ссылки существуют. Проверены пробелы нового документа и `git diff --check`. Код не менялся; тесты приложения/сборка в этой задаче не запускались.

При прежней подготовке проверка `.omx/verification/check-context.py` прошла: 6 канонических инструкций, 34 Markdown-файла контекста, 17 этапов плана (2 завершённых ранее +15 новых спецификаций), корректные ссылки и порядок зависимостей, покрытие M1–M5, отсутствие прежних шаблонных заглушек вне `_templates`. `git diff --check` прошёл.

Все три субагента провели независимую повторную проверку; замечания к данным/математике, UI и интеграциям исправлены ведущим. Финальная проверка аналитика и исследователя не выявила критичных противоречий в их областях; это проверка документации, не будущей реализации. Дополнительно русифицированы README репозитория и руководство композиции диалогов без изменения поведения компонентов.

В рамках анализа код приложения не менялся, сборка повторно не заявляется. Доказательства текущей задачи — чтение исходников/ZIP/XML, официальные страницы интеграций, проверка покрытия и согласованности документов. Полная сверка бухгалтерских итогов и персональных данных в каждом объекте Excel не выполнялась. Внешние сервисы не подключались.

## История и служебные замечания

- 23.09.2026: прежний запуск tmux Team завершился ошибкой `startup_rollback_pane_proof_unavailable`, до ACK/изменений; его артефакты не удалялись.02 затем реализован native-субагентами; этот старый сбой не блокирует новую документационную работу.
- Исходные `techtask/*`, пользовательские изменения и завершённые функциональные требования01/02 сохранены. Перевод01/02 меняет язык документа, не исторический объём.
- Прежнее замечание об исключении `/context` устарело: при проверке23.09.2026 `git ls-files context` показывает отслеживаемые документы, `git check-ignore` не находит исключения. `.gitignore` в задаче разделения работ не менялся; коммиты не создавались.

## 14 / 14a — результат ограниченного запуска

23.09.2026: работа остановлена по лимиту пользователя в пять минут; этап остаётся **в работе**, не завершён. Team `implement-features14-066252b1`: пять GPT-6-Astra/xhigh, запуск/ACK подтверждены. Частичные файлы сохранены в `.omx/verification/feature14/partial/`; полная интеграция, гонки, браузерный сценарий и сборка не подтверждены. Выявлены отсутствующие зависимости: реальный адаптер13 и сохраняемая политика точности/шага SKU. Добавлены закреплённые зависимости04; установка lock-only заблокирована политикой удалённых загрузок, lock взят из локальной ветки09 с согласованием корневого manifest. Совместимость CSV с1С не подтверждена.

Остановка Team завершена: runtime автоматически создал checkpoint/merge-коммиты и перенёс12 файлов из пяти рабочих копий в текущую ветку; состояние Team удалено. Реализация остановлена по таймбоксу, сохранение и завершение runtime заняли дополнительное время. Добавлены сервисы review/approval/export, четыре HTTP-маршрута и отдельная серверная панель проверки; панель не подключена к отсутствующему адаптеру13. Итоговое объединённое дерево не прошло полную приёмку; успешные базовые проверки до слияния не подтверждают новые возможности.

### Проверка перед условным push 14/14a

По запросу пользователя «push, если всё работает» отправка **не выполнена**. На объединённом дереве `npm run lint`, `npx tsc --noEmit` и существующие тесты auth/components/ai/realtime прошли; они не покрывают новые API14. `npm run build` завершился ошибкой Turbopack: локальный symlink node_modules ведёт за пределы корня файловой системы сборки. Критические функциональные блокеры подтверждены чтением кода: `saveReview` всегда отклоняет сохранение из-за отсутствия политики точности/шага SKU (`lib/server/reviews.ts:82`); `PersistedReview` нигде не подключён к странице/рабочему месту. Условие «всё работает» не выполнено. Новые изменения реализации и push не производились. Журналы: `.omx/verification/feature14/pre-push/`.

### Исправления14/14a перед push
В работе: атомарный PATCH с явной политикой SKU, подключение persisted UI, проверка сборки и push текущей ветки по поручению пользователя (четыре минуты).

### Исправления14/14a — поставка

Атомарное сохранение review реализовано с блокировками, одной версией на пакет и аудитом; BigInt проверяет точность/шаг без округления. Миграция `0001_review_quantity_policy.sql` добавляет явную nullable политику товара: существующие снимки не переписываются, для их изменения нужен новый импорт с `quantity_precision`/`quantity_step`. UI подключён через `/workspace?runId=...` и список собственных расчётов `/workspace/review`; исправлено восстановление после409. Утверждение проверяет configuration.runMode, экспорт не обновляет immutable source_objects при дедупликации. Turbopack учитывает общий корень symlink-зависимостей.

Проверки: lint и TypeScript прошли, production build прошёл после исправления корня.27 существующих серверных/контрактных тестов прошли; новый PostgreSQL API14 regression сначала выявил ошибку фикстуры (отсутствовала связь товар–поставщик), после исправления прошёл отдельно: сохранение, точность, чужой доступ, конкурентный конфликт, replay approval, инвалидирование экспорта и CSV. Ранее31 auth/UI/AI/realtime тест прошёл. Живые Clerk/S3 и браузерный E2E не проверены; импортёр09 должен записывать новую политику до публикации. Перед развёртыванием применить SQL-миграции через `node --conditions=react-server --import tsx scripts/migrate-data-model.ts`. Совместимость1С не заявляется.

### Разрешение конфликтов PR с main
По прямому запросу пользователя main объединён в текущую ветку; конфликтующие фрагменты сохранены со стороны PR (`-X ours`), неконфликтующие изменения main оставлены. Manifest/lock согласованы без дубликатов ключей. Проверка типов/линтера выполняется перед push; новые продуктовые правила не вводятся.

Проверки merge PR13: `next typegen`, `tsc --noEmit` прошли; lint без ошибок (устранён неиспользуемый import посредством динамической проверки списка миграций). PostgreSQL-проверки миграций и API14 прошли отдельно; первый параллельный запуск столкнулся с остановкой локального PostgreSQL. Конфликтные фрагменты UI сохранены со стороны PR; добавлен совместимый export getProjectDataPool.
